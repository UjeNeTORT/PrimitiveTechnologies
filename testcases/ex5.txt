in
pop     rax

push    1

call    Factorial

out

hlt

Factorial:  ; current n is stored in rax,
            ; result is stored in stack

    push    0
    push    rax

    je      Ret

    push    rax ; current n
    mul         ; multiply with value from stack

    push    rax
    push    1
    sub
    pop     rax

    call    Factorial

    Ret:

        ret

